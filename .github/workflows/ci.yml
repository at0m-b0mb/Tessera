name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  test:
    name: test (py${{ matrix.python }} on ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python: ["3.9", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}

      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install cryptography rich qrcode

      - name: Import check
        run: python -c "import tessera, tessera.core.manager, tessera.cli.main; print(tessera.__version__)"

      - name: Lifecycle tests
        run: python tests/test_lifecycle.py

      - name: Adoption tests
        run: python tests/test_adopt.py

      - name: Fleet, expiry, backup and drift tests
        run: python tests/test_v11.py

      - name: Security regression tests
        run: python tests/test_security.py

      - name: CLI smoke test against the simulated server
        # bash on every runner: the Windows default is PowerShell, where
        # `> /dev/null` and `|| true` are not valid.
        shell: bash
        run: |
          python -m tessera --version
          python -m tessera doctor demo
          python -m tessera demo reset
          python -m tessera install demo --yes --engine wireguard --dry-run
          python -m tessera install demo --yes -e wireguard -e openvpn --no-qr -o ./out
          python -m tessera status demo
          python -m tessera peer add guest --expires 14d -e wireguard --no-qr -o ./out demo
          python -m tessera peer list demo
          python -m tessera verify demo
          python -m tessera watch demo --once
          python -m tessera audit demo
          python -m tessera servers add ci root@ci.example.com
          python -m tessera servers list
          python -m tessera servers list --names
          python -m tessera servers remove ci
          python -m tessera completions bash > /dev/null
          python -m tessera completions zsh > /dev/null
          python -m tessera completions fish > /dev/null
          # Dry run before the real thing: afterwards there is nothing left to
          # remove and the command correctly exits non-zero.
          python -m tessera uninstall demo --dry-run
          python -m tessera uninstall demo -y

      - name: Generated configs must not be empty
        shell: bash
        run: |
          test -s ./out/wireguard-laptop.conf
          test -s ./out/openvpn-laptop.ovpn
          grep -q '^\[Interface\]' ./out/wireguard-laptop.conf
          grep -q 'PrivateKey' ./out/wireguard-laptop.conf
          grep -q '<ca>' ./out/openvpn-laptop.ovpn

  gui:
    name: gui (headless)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install
        run: |
          sudo apt-get update
          sudo apt-get install -y libegl1 libxkbcommon-x11-0 libdbus-1-3 \
            libxcb-icccm4 libxcb-image0 libxcb-keysyms1 libxcb-randr0 \
            libxcb-render-util0 libxcb-shape0 libxcb-xinerama0 libxcb-cursor0
          python -m pip install --upgrade pip
          pip install cryptography rich qrcode PyQt6
      - name: Render the GUI offscreen
        env:
          QT_QPA_PLATFORM: offscreen
        run: python scripts/screenshots.py /tmp/shots
      - name: Every screenshot must exist and be non-trivial
        run: |
          ls -l /tmp/shots
          for f in /tmp/shots/*.png; do
            size=$(stat -c%s "$f")
            echo "$f: $size bytes"
            test "$size" -gt 8000 || { echo "suspiciously small: $f"; exit 1; }
          done
      - uses: actions/upload-artifact@v4
        with:
          name: screenshots
          path: /tmp/shots

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install ruff
      - run: ruff check tessera/ --output-format=github
        continue-on-error: true

  py39-compat:
    name: python 3.9 syntax compatibility
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.9"
      - name: Compile every module under 3.9
        run: python -m compileall -q tessera/ scripts/ tests/
